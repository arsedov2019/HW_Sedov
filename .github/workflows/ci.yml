name: C/C++ CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Configure CMake
      run: cmake -B build -S 05_hw -DCMAKE_C_FLAGS="-fsanitize=address,undefined,leak -g"

    - name: Install dependencies
      run: sudo apt-get install -y gcc g++ cmake

    - name: Configure CMake
      run: cmake -B build -DCMAKE_C_FLAGS="-fsanitize=address,undefined,leak -g" .

    - name: Build project
      run: cmake --build build

    - name: Run tests
      run: ./build/test_stack
